import React from 'react';

function Table(  ) {

 
  return (
    <>
    {/* <table className="table">
  <thead>
    <tr>
      <th scope="col">id</th>
      <th scope="col">UserName</th>
      <th scope="col">Pasword</th>
      <th scope="col">Actions</th>
     
    </tr>
  </thead>
  <tbody>
    {
      users.map((user,index) => {
        return (
          <tr key={index}>
           
            <th scope="row">{user.id}</th>
            <td>{user.email}</td>
            <td>{user.password}</td>
  <td><button  className="btn btn-primary my-1" onClick={()=>handleEdit(user.id)}>Edit</button></td>
          </tr>
        )
      })
    }
    
   
  </tbody>
</table> */}
    </>
  )
}

export default Table